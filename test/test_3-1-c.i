# 1 "test/test_3-1-c.c"
# 1 "<built-in>"
# 1 "<command-line>"
# 31 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 32 "<command-line>" 2
# 1 "test/test_3-1-c.c"
# 1 "/usr/lib/gcc/x86_64-linux-gnu/8/include/stdbool.h" 1 3 4
# 2 "test/test_3-1-c.c" 2

int main(int argc, char **argv)
{
    int x = 5;
    
# 6 "test/test_3-1-c.c" 3 4
   _Bool 
# 6 "test/test_3-1-c.c"
        y = 
# 6 "test/test_3-1-c.c" 3 4
            0
# 6 "test/test_3-1-c.c"
                 ;

    x = x + 1;
    x = x - 1;
    x = x * 1;
    x = x / 1;
    x = x % 1;
    x = x << 1;
    x = x >> 1;
    x = x & 1;
    x = x ^ 1;
    y = x < 7;
    y = x <= 7;
    y = x > 7;
    y = x >= 7;
    y = x == 5;
    y = x != 7;
    y = y && 
# 23 "test/test_3-1-c.c" 3 4
            0
# 23 "test/test_3-1-c.c"
                 ;
    y = y || 
# 24 "test/test_3-1-c.c" 3 4
            0
# 24 "test/test_3-1-c.c"
                 ;

    x = x+ 1;
    x = x- 1;
    x = x* 1;
    x = x/ 1;
    x = x% 1;
    x = x<< 1;
    x = x>> 1;
    x = x& 1;
    x = x^ 1;
    y = x< 7;
    y = x<= 7;
    y = x> 7;
    y = x>= 7;
    y = x== 5;
    y = x!= 7;
    y = y&& 
# 41 "test/test_3-1-c.c" 3 4
           0
# 41 "test/test_3-1-c.c"
                ;
    y = y|| 
# 42 "test/test_3-1-c.c" 3 4
           0
# 42 "test/test_3-1-c.c"
                ;

    x = x +1;
    x = x -1;
    x = x *1;
    x = x /1;
    x = x %1;
    x = x <<1;
    x = x >>1;
    x = x &1;
    x = x ^1;
    y = x <7;
    y = x <=7;
    y = x >7;
    y = x >=7;
    y = x ==5;
    y = x !=7;
    y = y &&
# 59 "test/test_3-1-c.c" 3 4
           0
# 59 "test/test_3-1-c.c"
                ;
    y = y ||
# 60 "test/test_3-1-c.c" 3 4
           0
# 60 "test/test_3-1-c.c"
                ;
}
